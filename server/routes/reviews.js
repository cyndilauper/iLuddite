const express = require('express');
const router = express.Router();
const Reviews = require('../models/reviews');
const request = require('request');

router.route('/reviews/:bookid')

  .get((req, res, next) => {
    
  })

  .post((req, res, next) => {
    
  })



module.exports = router;
